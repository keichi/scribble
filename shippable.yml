language: go

go:
  - 1.3
  - 1.4

install:
  - go get github.com/jstemmer/go-junit-report
  - go get -d -t -v ./...
  - go build -v ./...

script:
  - go test -v ./... | go-junit-report > shippable/testresults/junit.xml

